@model IEnumerable<sphinxsolaries.Caems.Models.CAEMS_authenticate_User3>
 
 
<div class="card">
    <div class="card-body">
        <h4 class="card-title">View Users</h4> 
        <div class="table-responsive">
            <table class="table table-striped table-bordered zero-configuration">
                <thead>
                    <tr>
                        <th>   </th> 
                        <th>first name</th>
                        <th>last name</th>
                        <th>email</th>
                        <th>role</th>
                        @*<th>password</th>
                        <th>password2</th>
                        <th>Organization</th>
                        <th>Lead User</th>
                        <th>Service Company</th>*@
 
                    </tr> 
                </thead>
                <tbody>
                    @foreach (var item in Model)
                    {
                        <tr>                            
                            <td>@Html.ActionLink("Select", "edit_Users", new
                           {
                            id=item.Id, 
                            Service_Company = item.Service_company
                            ,Lead_User = item.Lead_user
                            ,Organization = item.Organization
                            ,password2 = item.Password2
                            ,password = item.Password
                            ,role = item.Role
                            ,email = item.Email
                            ,last_name = item.Last_name
                            ,first_name = item.First_name
                               })</td> 
                           <td>@Html.DisplayFor(modelItem => item.First_name)</td>
                           <td>@Html.DisplayFor(modelItem => item.Last_name)</td>
                           <td>@Html.DisplayFor(modelItem => item.Email)</td>
                           <td>@Html.DisplayFor(modelItem => item.Role2)</td>
                           @*<td>@Html.DisplayFor(modelItem => item.Password)</td>
                           <td>@Html.DisplayFor(modelItem => item.Password2)</td>
                           <td>@Html.DisplayFor(modelItem => item.Organization)</td>
                           <td>@Html.DisplayFor(modelItem => item.Lead_user)</td>
                           <td>@Html.DisplayFor(modelItem => item.Service_company)</td>*@
                        </tr>
                    }
                    
                </tbody>
                <tfoot>
                    <tr>                        
                        <th>   </th> 
                        <th>first name</th>
                        <th>last name</th>
                        <th>email</th>
                        <th>role</th>
                        @*<th>password</th>
                        <th>password2</th>
                        <th>Organization</th>
                        <th>Lead User</th>
                        <th>Service Company</th>*@
 
                    </tr>
                </tfoot>
            </table>
        </div>
    </div>
</div> 
<script> 
    function msg(txt) {
        sweetAlert("...", txt, "info");
    }
    var statusMessage = "@ViewBag.status" || false; 
    if (statusMessage != false && statusMessage.length > 0) {
        msg(statusMessage); 
    }
</script>