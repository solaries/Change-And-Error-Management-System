@model IEnumerable<sphinxsolaries.Caems.Models.CAEMS_Change_Or_Error2>
 
 
<div class="card">
    <div class="card-body">
        <h4 class="card-title">View Change Or Errors</h4> 
        <div class="table-responsive">
            <table class="table table-striped table-bordered zero-configuration">
                <thead>
                    <tr>
                        <th>   </th>
                        <th>Project</th>
                        @**@
                        <th>Initiating User Type</th>
                        <th>User</th>
                        <th>Change Or Error Detail</th>
                        <th>Log Date</th>

 
                    </tr> 
                </thead>
                <tbody>
                    @foreach (var item in Model)
                    {
                        <tr>   
                            

                            <td>
                                @Html.ActionLink("Select", "edit_Change_Or_Errors", new
                           {
                               id = item.Id,
                               Log_Date = item.Log_date
                            ,
                               Change_Or_Error_Detail = item.Change_or_error_detail
                                   //,User = item.User
                                   //,Service_Company = item.Service_company
                            ,
                               Project = item.Project
                           })
                        </td>
                        <td>@Html.DisplayFor(modelItem => item.Project2)</td>
                        @**@
                        <td>@Html.DisplayFor(modelItem => item.Initiating_User_Type2)</td>
                        <td>@Html.DisplayFor(modelItem => item.User2)</td>
                        <td>@Html.DisplayFor(modelItem => item.Change_or_error_detail)</td>
                        <td>@Html.DisplayFor(modelItem => item.Log_date)</td>
                            
                                                     
                            @*<td>@Html.ActionLink("Select", "edit_Change_Or_Errors", new
                           {
                            id=item.Id, 
                            Log_Date = item.Log_date
                            ,Change_Or_Error_Detail = item.Change_or_error_detail
                            ,User = item.User 
                            ,Project = item.Project
                               })</td> 

                            
                           <td>@Html.DisplayFor(modelItem => item.Project2)</td>
                          
                            <td>@Html.DisplayFor(modelItem => item.Initiating_User_Type2)</td>
                           <td>@Html.DisplayFor(modelItem => item.User2)</td>
                           <td>@Html.DisplayFor(modelItem => item.Change_or_error_detail)</td>
                           <td>@Html.DisplayFor(modelItem => item.Log_date)</td>*@


                        </tr>
                    }
                    
                </tbody>
                <tfoot>
                    <tr>   
                        <th>   </th>
                        <th>Project</th>
                        @**@
                        <th>Initiating User Type</th>
                        <th>User</th>
                        <th>Change Or Error Detail</th>
                        <th>Log Date</th>
 
                    </tr>
                </tfoot>
            </table>
        </div>
    </div>
</div> 
<script> 
    function msg(txt) {
        sweetAlert("...", txt, "info");
    }
    var statusMessage = "@ViewBag.status" || false; 
    if (statusMessage != false && statusMessage.length > 0) {
        msg(statusMessage); 
    }
</script>